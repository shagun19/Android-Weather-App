<?xml version="1.0" encoding="utf-8"?>



<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/bookmarkPage_Card"
    android:layout_width="185dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_gravity="center_horizontal"
    app:cardCornerRadius="10dp"
    android:layout_margin="5dp"
    android:layout_height="wrap_content">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/bookmarkPage_image"
            android:layout_width="match_parent"
            android:layout_height="150dp"
            android:layout_gravity="center_horizontal"
            android:scaleType="centerCrop"
            android:src="@drawable/cloudy_weather" />
        <TextView
            android:id="@+id/bookmarkPage_headline"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:textSize="18sp"
            android:ellipsize="end"
            android:maxLines="3"
            android:scrollHorizontally="false"
            android:singleLine="false"
            android:layout_margin="4dp"
            android:textColor="@color/colorAccent"
            android:textStyle="bold"
            android:text="headline title"/>
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">
            <TextView
                android:id="@+id/bookmarkPage_section"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="start"
                android:layout_margin="4dp"
                android:text="date and section"/>
            <ImageView
                android:id="@+id/bookmarkPage_bookmark"
                android:layout_height="wrap_content"
                android:layout_width="wrap_content"
                android:layout_marginLeft="165dp"
                android:layout_alignBottom="@+id/bookmarkPage_section"
                android:layout_gravity="end"
                android:src="@drawable/ic_bookmark_black_24dp"/>
        </RelativeLayout>
    </LinearLayout>



</androidx.cardview.widget.CardView>
